services:
  dev:
    build:
      context: ..
      dockerfile: .saturn/Dockerfile
    image: localhost/saturn-human-llm:latest
    container_name: saturn_human-llm
    init: true
    working_dir: /root/human-llm
    command: ["sleep", "infinity"]
    environment:
      SATURN_IN_GUEST: "1"
      SATURN_SOCK: /var/run/docker.sock
      IS_SANDBOX: "1"
    volumes:
      - ..:/root/human-llm
      - ${HOME}/.claude:/root/.claude
      - ${HOME}/.claude.json:/root/.claude.json
      - ${HOME}/.config/gh:/root/.config/gh
